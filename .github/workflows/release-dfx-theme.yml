name: release dfx-theme

on:
  push:
    tags:
      - 'dfx-theme**'

permissions:
  id-token: write # Required for OIDC
  contents: write # Required for GitHub release action

jobs:
  dfx-theme:
    uses: ./.github/workflows/release.yml
    with:
      lib: dfx-theme
