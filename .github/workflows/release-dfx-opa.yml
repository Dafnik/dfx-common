name: release dfx-opa

on:
  push:
    tags:
      - 'dfx-opa**'

permissions:
  id-token: write # Required for OIDC
  contents: write # Required for GitHub release action

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: test
        uses: ./.github/actions/test
        with:
          library: dfx-opa

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: setup node and pnpm
        uses: dafnik/setup-node-pnpm@v5
        with:
          install: true

      - name: build dfx-opa
        run: pnpm ng build dfx-opa

      - name: zip dfx-opa
        run: |
          cd ./dist/
          zip -r "dfx-opa.zip" ./dfx-opa
          cd -

      - uses: actions/upload-artifact@v7
        name: create dfx-opa artifact
        with:
          name: dfx-opa
          path: ./dist/dfx-opa.zip
          retention-days: 1

  release-github:
    name: Release zip to github releases
    needs: [build, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/download-artifact@v8
        with:
          name: dfx-opa
          path: ./dist

      - name: get dfx-opa version
        id: get-version
        shell: bash
        run: echo "VERSION=$(node -p "require('./libs/dfx-opa/package.json').version")" >> $GITHUB_OUTPUT

      - name: set to versioned zip dfx-opa-v${{ steps.get-version.outputs.VERSION }}.zip
        shell: bash
        run: |
          mkdir releases
          cp "./dist/dfx-opa.zip" "./releases/dfx-opa-v${{ steps.get-version.outputs.VERSION }}.zip"

      - name: releases lookup
        run: |
          cd releases
          ls -la
          du -hs
          cd -

      - name: get only latest changelog
        run: |
          ./tools/extractLatestVersionChangelog.sh ./libs/dfx-opa/CHANGELOG.md
          cat RELEASE_CHANGELOG.md

      - name: create dfx-opa-v${{ steps.get-version.outputs.VERSION }} release
        uses: softprops/action-gh-release@v2.6.1
        with:
          body_path: ./RELEASE_CHANGELOG.md
          files: ./releases/dfx-opa-v${{ steps.get-version.outputs.VERSION }}.zip

  release-npm:
    name: Release to npm registry
    needs: [build, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          registry-url: 'https://registry.npmjs.org'
          node-version: '24'

      - uses: actions/download-artifact@v8
        with:
          name: dfx-opa
          path: ./dist

      - name: unzip
        shell: bash
        run: |
          cd dist
          unzip dfx-opa.zip
          cd -

      - name: dist lookup
        shell: bash
        run: |
          cd dist/dfx-opa
          ls -la
          du -hs
          cd -

      - name: publish to npm registry
        env:
          NPM_CONFIG_PROVENANCE: true
        run: |
          cd dist/dfx-opa/
          npm publish
