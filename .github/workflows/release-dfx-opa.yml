name: release dfx-opa

on:
  push:
    tags:
      - 'dfx-opa**'

permissions:
  id-token: write # Required for OIDC
  contents: write # Required for GitHub release action

jobs:
  dfx-opa:
    uses: ./.github/workflows/release.yml
    with:
      lib: dfx-opa
